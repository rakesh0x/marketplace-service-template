# ─── YOUR WALLET (where USDC payments arrive) ───
# Solana wallet address (required)
WALLET_ADDRESS=6eUdVwsPArTxwVqEARYGCh4S2qwW2zCs7jSEDRpxydnv
# Base wallet address (EVM 0x... address)
# If not set, defaults to WALLET_ADDRESS (but fails if WALLET_ADDRESS is Solana format)
WALLET_ADDRESS_BASE=0xF8cD900794245fc36CBE65be9afc23CDF5103042

# ─── SERVICE CONFIG ───
SERVICE_NAME=prediction-market-aggregator
SERVICE_DESCRIPTION=Real-time prediction market aggregator with social sentiment signals using mobile proxies.
PRICE_USDC=0.05

# Google Reviews & Business Data API (available at /api/reviews/*, /api/business/*)
REVIEWS_PRICE_USDC=0.02
BUSINESS_PRICE_USDC=0.01
SUMMARY_PRICE_USDC=0.005

PORT=3000
RATE_LIMIT=60

# ─── PROXY CREDENTIALS ───
# Get from https://client.proxies.sx or buy via x402 API
PROXY_HOST=
PROXY_HTTP_PORT=
PROXY_USER=
PROXY_PASS=
PROXY_COUNTRY=US

# ─── BROWSER API (Twitter Sentiment Scraping) ───
# Required for Twitter sentiment signals
BROWSER_ENDPOINT=https://browser.proxies.sx
BROWSER_PAYMENT_SIG=

# ─── OPTIONAL: Custom RPC endpoints ───
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# BASE_RPC_URL=https://mainnet.base.org
