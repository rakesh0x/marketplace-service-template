# ─── YOUR WALLET (where USDC payments arrive) ───
# Solana wallet address (required)
WALLET_ADDRESS=
# Base wallet address (optional — defaults to WALLET_ADDRESS if not set)
WALLET_ADDRESS_BASE=

# ─── SERVICE CONFIG ───
SERVICE_NAME=gmail-account-creator
SERVICE_DESCRIPTION=Create Gmail accounts using real mobile IPs and antidetect browser.
PRICE_USDC=0.50
PORT=3000
RATE_LIMIT=60

# ─── PROXY CREDENTIALS ───
# Get from https://client.proxies.sx or buy via x402 API:
#   curl https://api.proxies.sx/v1/x402/proxy?country=US&traffic=1
PROXY_HOST=
PROXY_HTTP_PORT=
PROXY_USER=
PROXY_PASS=
PROXY_COUNTRY=US

# ─── ANTIDETECT BROWSER (required for Gmail Account Creator) ───
# Get internal key from browser.proxies.sx team
BROWSER_ENDPOINT=https://browser.proxies.sx
BROWSER_INTERNAL_KEY=

# ─── OPTIONAL: Custom RPC endpoints ───
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# BASE_RPC_URL=https://mainnet.base.org
